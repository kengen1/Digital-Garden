## 📝 Problem Details

- **Title:** `994. Rotting Oranges`
- **Link:** https://leetcode.com/problems/rotting-oranges/
- **Difficulty:** #Medium 
- **Tags/Categories:** #Graphs 

> input matrix `grid` of size `m * n`
> each cell in `grid` can have three possible values:
> 	`0` --> empty cell
> 	`1` --> fresh orange
> 	`2` --> rotten orange
> every minute any fresh orange that is **4-directionally adjacent** to a rotten orange becomes rotten
> return the min number of minutes that must elapse until there are no cells with a fresh orange
> return -1 if impossible 
## 💭What Were My Initial Thoughts?

```
        // base case:
        // init traversal variables
        // init BFS queue
        // first pass through to locate the positions of the rotten oranges
        // push the location of each (2) into the queue
        // init directions vector (up,down,left,right)
        // BFS while the queue is not empty
            // keep a counter for the number of minutes (1 min == 1 while loop iteration)
        // get row and col variables from front of queue
        // pop queue
        // try move 1 in each direction from this rotten orange position
            // if its a fresh orange, add it to the queue
            // changes its value to rotten (2)
        // return counter
```

## 🤔What Did I Struggle With?

```

```

## 💡 Explanation of Solution

```

```

## ⌛ Complexity Analysis

```

```

## 💻 Implementation of Solution

```cpp

```